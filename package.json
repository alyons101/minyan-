{
  "name": "minyan-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile",
    "server",
    "web"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspaces",
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web && mkdir -p server/public && cp -r web/dist/* server/public/ && npm run build -w server",
    "start": "npm run start -w server",
    "seed": "npm run seed -w server",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
